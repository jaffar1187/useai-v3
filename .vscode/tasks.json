{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-daemon",
      "type": "shell",
      "command": "pnpm build",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": ["$tsc"]
    }
  ]
}
